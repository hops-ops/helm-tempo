apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: tempos.helm.hops.ops.com.ai
spec:
  group: helm.hops.ops.com.ai
  names:
    kind: Tempo
    plural: tempos
  scope: Namespaced
  versions:
  - name: v1alpha1
    referenceable: true
    served: true
    schema:
      openAPIV3Schema:
        description: Tempo installs the Grafana Tempo Helm chart for distributed tracing.
        type: object
        properties:
          spec:
            description: TempoSpec defines the desired state of Tempo.
            type: object
            properties:
              clusterName:
                description: Name of the target cluster. Used as default for provider config names.
                type: string
              managementPolicies:
                description: Management policies for all composed resources. Defaults to ["*"].
                type: array
                items:
                  type: string
                default:
                - "*"
              labels:
                description: Custom labels applied to all managed resources. Merged with default labels (hops.ops.com.ai/managed, hops.ops.com.ai/tempo).
                type: object
                additionalProperties:
                  type: string
                x-kubernetes-preserve-unknown-fields: true
              providerConfigRef:
                description: Reference to the Helm ProviderConfig.
                type: object
                properties:
                  name:
                    description: Name of the ProviderConfig. Defaults to clusterName.
                    type: string
                  kind:
                    description: Kind of the ProviderConfig. Defaults to ProviderConfig.
                    type: string
                    enum:
                    - ProviderConfig
                    - ClusterProviderConfig
              namespace:
                description: Namespace for the Helm release. Defaults to tempo.
                type: string
              name:
                description: Helm release name. Defaults to the XR metadata.name.
                type: string
              values:
                description: Helm values passed directly to the chart.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              overrideAllValues:
                description: Helm values passed directly to the chart. Use when a parent composition needs to bypass its own defaults.
                type: object
                x-kubernetes-preserve-unknown-fields: true
          status:
            description: TempoStatus defines the observed state of Tempo.
            type: object
            properties:
              ready:
                description: Overall readiness of the Tempo installation.
                type: boolean
              release:
                description: Status of the Helm release.
                type: object
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  ready:
                    type: boolean
        required:
        - spec
